# Docker Compose 环境变量
# 复制此文件为 .env 并填入实际值

MYSQL_ROOT_PASSWORD=replace_with_strong_password
MYSQL_DATABASE=board_cutting_db
JWT_SECRET=replace_with_base64_encoded_secret_key
BACKEND_PORT=8080
NGINX_PORT=80
