# 柜门板材切割排版系统 — 拓展路线图

> 编写日期: 2026-05-07 | 最后更新: 2026-05-09
> 当前版本基线: 单体 CRUD + 多算法排样 + 订单状态机 + RBAC + 审计 + Docker + Excel
> 目标: 从"工具"演进为"平台"

---

## 一、核心定位

本系统本质是**切割优化引擎 + 生产执行系统**。算法决定材料利用率（省钱），生产管理决定车间效率（省时间）。所有其他功能服务于这两个核心。

**优先级原则：先打磨刀刃（算法+生产），再装修刀鞘（可视化+集成）。**

---

## 二、分层路线图

```
┌───────────────────────────────────────────────────────┐
│  ③ 体验与集成：小程序/PWA/二维码/ERP/CAD/ES              │
│  ┌─────────────────────────────────────────────────┐  │
│  │  ② 数据驱动：ECharts看板/报表/数据大屏              │  │
│  │  ┌───────────────────────────────────────────┐  │  │
│  │  │  ① 核心业务闭环                             │  │  │
│  │  │  ┌───────────────────────────────────┐    │  │  │
│  │  │  │  已完成基础：RBAC/审计/Docker/Excel  │    │  │  │
│  │  │  └───────────────────────────────────┘    │  │  │
│  │  └───────────────────────────────────────────┘  │  │
│  └─────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────┘
```

---

## 三、已完成基础（4/4 ✅）

| 模块 | 说明 |
| --- | --- |
| ✅ RBAC 权限体系 | 三角色(admin/operator/viewer) + 9权限码 + @RequirePermission + v-permission |
| ✅ 操作审计日志 | AOP @AuditLog + @Async 异步写入，覆盖7控制器全部写操作 |
| ✅ Docker Compose | MySQL + Nginx + Backend 三容器编排，多阶段 Dockerfile |
| ✅ EasyExcel 导入导出 | 客户/板材批量导入导出+模板下载，审计日志导出 |

---

## 四、第 1 层：核心业务闭环 🔥

> **目标**: 让系统从"CRUD 台账"变成"切割生产引擎"

### 1.1 订单生命周期状态机 ✅

```
待审核(0) → 已审核(1) → 排样中(2) → 已排样(3) → 生产中(4) → 已完工(5) → 已交付(6)
  ↓           ↓           ↓           ↓           ↓           ↓
 驳回(-1)    取消(-2)    排样失败(-3)  重新排样     暂停        返工
```

- 10 状态枚举 `OrderStatus` + `allowedNext` 集合声明式守卫
- `TOrderServiceImpl.transitionStatus()` 转换校验 + `statusHistory` JSON 列记录历史
- `PUT /orders/{id}/status` + `GET /orders/status-labels` REST API
- **价值**: 订单不再是一行死数据，而是可追踪的活流程

### 1.2 算法升级：多算法 + 异步 ✅

- 策略模式 `CuttingAlgorithm` 接口，`TabuSearch` + `GeneticAlgorithm` 双算法
- `AlgorithmRegistry` 工厂注册表，前端可选算法并对比利用率 (`POST /algorithm/compare`)
- 异步求解: `AlgorithmTask` 持久化任务，`AlgorithmServiceImpl.submit()` 提交 → 轮询结果 ⚠️ TaskRunner 因 MyBatis-Plus/CGLIB 代理冲突已删除，当前同步执行
- **价值**: 核心竞争壁垒。多算法可选对比，准确率提升

### 1.3 生产任务模块 ⬜

- 排样结果确认后自动生成"切割任务"
- 任务分配工人、预估/实际工时
- Kanban 看板: 待生产 → 生产中 → 已完成
- **价值**: 打通"排样 → 执行"，解决数据断层

### 1.4 数据分析看板 ⬜

- 重写 Dashboard：今日订单数、材料利用率、切割任务完成率、不良品率
- 近 7/30 天趋势折线图
- 技术: ECharts (vue-echarts)
- **价值**: 打开系统第一眼就看到业务全貌，不再只有 3 个数字

---

## 五、第 2 层：业务增强

> **目标**: 精细化管控制造成本和质量

### 2.1 余料智能管理 ⬜

- 切割任务完成后自动登记余料
- 新订单排样时优先匹配合适余料
- 余料利用率统计 + 低利用率告警

### 2.2 质量管理 ⬜

- 质检记录（合格/不合格/返工原因）
- 不良品率按板材类型、按工人维度统计
- 关联切割任务 + 拍照留证

### 2.3 成本核算 ⬜

- 板材成本 + 人工成本 + 损耗成本
- 按订单/客户/时间段核算利润
- 毛利率 + 材料利用率双维度评价

### 2.4 文件管理 ⬜

- 板材照片、订单附件、设计图纸上传
- 技术: MinIO 对象存储

---

## 六、第 3 层：体验与集成

> **目标**: 移动端覆盖 + 外部系统对接

### 3.1 小程序增强 ⬜

- 扫码开工、拍照上传、离线缓存、消息推送

### 3.2 二维码 / 条形码 ⬜

- 订单/任务/板材生成二维码，打印贴现场，扫码定位

### 3.3 报表中心 ⬜

- 日报/周报/月报，PDF 导出
- 技术: 前端 `html2canvas` + `jspdf`

### 3.4 数据大屏 ⬜

- 全屏页面，车间电视展示，实时刷新

### 3.5 ERP / MES Webhook ⬜

- REST API + API Key 认证，订单状态变更通知外部系统

### 3.6 消息通知中心 ⬜

- 站内信 + 邮件 + 小程序订阅消息
- Spring Event + 策略模式多通道

---

## 七、第 4 层：锦上添花

> **目标**: 在核心扎实后再做的增强

- **排样"断点续排"**: 前端 Canvas 拖拽微调 + 增量求解
- **3D 排样预览**: Three.js 渲染 + 利用率热力图
- **AI 辅助推荐**: 历史数据推荐板材规格，预测利用率
- **CAD 文件对接**: DXF/DWG 导入解析
- **Elasticsearch 搜索**: 板材/订单模糊搜索、拼音搜索
- **PWA**: 网页端离线安装到手机桌面

---

## 八、建议实施顺序

```
🔥 当前批次 — 核心业务闭环 (最优先)
├── 1.1 订单生命周期状态机 ✅
├── 1.2 算法升级（多算法 + 异步）✅ (异步部分回退)
├── 1.3 生产任务模块 (Kanban)   ← 下一步
└── 1.4 数据分析看板 (ECharts)

第 2 批 — 业务增强
├── 2.1 余料智能管理
├── 2.2 质量管理
├── 2.3 成本核算
└── 2.4 文件管理 (MinIO)

第 3 批 — 体验与集成
├── 3.1 小程序增强
├── 3.2 二维码/条形码
├── 3.3 报表中心 + PDF
├── 3.4 数据大屏
├── 3.5 ERP Webhook
└── 3.6 消息通知中心

第 4 批 — 锦上添花（按需启动）
├── 排样断点续排 / 3D预览 / AI推荐 / CAD对接 / ES搜索 / PWA
```

---

## 九、技术栈升级清单

| 类别 | 当前 | 建议引入 | 用途 | 状态 |
| --- | --- | --- | --- | --- |
| 权限 | 无 | 自研轻量方案 | RBAC | ✅ |
| 容器 | 无 | Docker Compose | 一键部署 | ✅ |
| 异步 | 无 | Spring @Async | 审计+排样 | ✅审计/待排样 |
| 图表 | 无 | ECharts | 看板 & 报表 | ⬜ |
| 文件存储 | 无 | MinIO | 图片/附件 | ⬜ |
| 实时通信 | 无 | WebSocket | 排样进度推送 | ⬜ |
| 搜索 | SQL LIKE | ES（按需） | 全文检索 | ⬜ |
| 二维码 | 无 | ZXing | 扫码识别 | ⬜ |
| PWA | 无 | vite-plugin-pwa | 离线访问 | ⬜ |
| 3D | 无 | Three.js | 3D预览 | ⬜ |

---

## 十、完成记录

| 日期 | 模块 | 说明 |
| --- | --- | --- |
| 2026-05-07 | RBAC 权限体系 | 三角色/9权限码/@RequirePermission+v-permission；48文件 |
| 2026-05-07 | 操作审计日志 | AOP+@Async；7控制器全覆盖；164测试 |
| 2026-05-07 | Docker Compose | MySQL+Nginx+Backend三容器；多阶段Dockerfile |
| 2026-05-07 | EasyExcel 导入导出 | 客户/板材批量导入导出+模板；164测试 |
| 2026-05-07 | 订单生命周期状态机 | 10状态枚举+allowedNext守卫+statusHistory JSON+转换API；OrderModuleTest |
| 2026-05-07 | 多算法策略模式 | CuttingAlgorithm接口+TabuSearch+GeneticAlgorithm+AlgorithmRegistry+compare端点 |
| 2026-05-07 | 异步算法回退 | TaskRunner 因 MyBatis-Plus@ServiceImpl CGLIB 代理冲突删除，改为同步执行 |
