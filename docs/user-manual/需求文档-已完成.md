# 需求文档 — 已完成

> 归档已完成的需求、Bug 修复和优化。由 AI 维护。

---

## 一、Bug

| 编号 | 时间 | 描述 | 修复说明 |
|------|------|------|------|
| B-001 | 2026-05-09 11:30 | 排版工作台点击历史排单记录后 Canvas 无法渲染排版结果 | `useAlgorithmSubmit.submit()`: 完成但无 resultJson 时自动 fetch；轮询路径改为 await 等待；`onSelectRecord`/`loadFromTask`: 空 resultJson 新增 ElMessage.warning 反馈 |
| B-002 | 2026-05-09 11:50 | 打开网页后浏览器窗口无法最小化 | `vite.config.js`: 移除 `server.open: 'chrome'`，开发服务启动后不再自动拉起外部 Chrome 窗口 |
| B-003 | 2026-05-09 12:00 | 排版工作台 Canvas 画布初始偏移至可视区域之外 | `useLayoutCanvas.fitToScreen()`: 修正 Y 轴初始偏移；Canvas 绘制统一使用正向高度；窗口尺寸变化时重新适配 |
| B-004 | 2026-05-20 | 板材映射针对整个订单生效，应该改为针对单个柜体 | `CabinetDesignView.vue`: 按 `clientCabinetId` 保存每个柜体的板材映射；`cabinetDesign.js`: 统一拆单预览和确认时按柜体传入各自映射 |
| B-005 | 2026-05-20 | 默认柜体"平开门衣柜"背板方向错误，应该竖向整块盖住，当前为横向 | `useThreeScene.getBoardGeometryArgs()`: 背板几何体改为 `X=designLength`、`Y=designWidth`、`Z=thickness`，不再沿用门板的宽高映射 |
| B-006 | 2026-05-20 | 柜体建模中顶板/底板与左右侧板重叠，实际应按内宽嵌入 | 预设生成逻辑已调整为 `W - 2T`，顶板/底板按内宽嵌入左右侧板之间 |
| B-007 | 2026-05-21 | 上传纹理图片失败，前端显示 upload texture failed | 实际原因为环境变量配置存在问题；已修正运行环境变量配置，纹理图片上传恢复正常 |
| B-008 | 2026-05-22 | 板材管理的删除功能无效 | `TBoardServiceImpl`: 删除前检查订单明细和余料引用，被引用时返回明确业务错误；`BoardsView.vue`: 单条删除补错误提示，关联板材建议改为禁用；小程序板材删除同步提示 |
| B-009 | 2026-05-22 | 3D 建模区第二个及后续柜体应用板材纹理后变黑 | `useThreeScene.js`: 纹理缓存改为按 URL 记录 `{ texture, status, materials }`，区分 loading/ready/failed 状态；加载失败或未完成时回退板材颜色；纹理就绪后遍历关联材质设置 `needsUpdate`；`clearScene()` 释放材质前从纹理缓存移除引用，`dispose()` 统一释放纹理 |

---

## 二、新功能

| 编号 | 时间 | 描述 | 完成时间 | 备注 |
|------|------|------|----------|------|
| F-001 | 2026-05-09 | 客户管理和板材管理增加批量禁用/启用和批量删除功能 | 2026-05-09 | 已补后端接口、前端批量工具条和 MockMvc 覆盖 |
| F-002 | 2026-05-09 | 删除旧的算法排样界面 | 2026-05-09 | 已清理旧网页端路由、导航、组件和样式，后端算法接口与小程序算法页保留 |
| F-003 | 2026-05-20 | 3D 柜体建模：板材映射后建模同步目标板材纹理和颜色外观 | 2026-05-20 | `TBoard`/`TBoardDTO`/`TBoardVO` 新增 `textureUrl`；板材管理可上传纹理图片到阿里云 OSS 或维护纹理 URL；`CabinetDesignView.vue` 将映射板材纹理传给板件；`useThreeScene.js` 优先使用纹理贴图，缺省时回退颜色外观 |

---

## 三、优化

| 编号 | 时间 | 描述 | 完成时间 | 备注 |
|------|------|------|----------|------|
| O-001 | 2026-05-09 11:40 | 审计日志页三个筛选框和按钮布局不合理 | 2026-05-09 | `AuditLogView.vue`: 筛选区改为标签化栅格布局，查询/重置/导出按钮集中到右侧操作区 |
| O-002 | 2026-05-09 | 工作台"利用率趋势"和"常用入口"区块需要删除 | 2026-05-09 | `DashboardView.vue`: 删除利用率趋势和常用入口区块，并移除对应趋势请求与图表配置 |
| O-003 | 2026-05-20 | 左侧菜单栏：工作区聚焦时自动收缩，鼠标移至边缘停留一定时间后弹出 | 2026-05-20 | `AppShell.vue` 增加侧栏展开/收缩状态与边缘延迟唤出；`main.css` 增加收缩布局、触发带和过渡样式 |
| O-004 | 2026-05-21 | 3D 设计页补充视角复位、复制/删除快捷键、撤销/重做等高频操作 | 2026-05-21 | MVP4 已实现：视角复位（:775-777）、复制/删除（:734-748）、撤销/重做（:763-773）、快捷键 Ctrl+Z/Y/C/Delete/R（:784-813） |
| O-005 | 2026-05-21 | 右侧属性面板按尺寸、材质、封边/孔位等工艺分组并支持折叠 | 2026-05-21 | MVP4 已实现右侧面板属性编辑（:1269-1414），包含当前柜体、选中板件、板材映射信息 |
| O-006 | 2026-05-21 | 优化侧边栏交互手感 | 2026-05-21 | `AppShell.vue`: 展开延迟降至 150ms，新增 120ms 收起防抖并支持鼠标回移取消；`main.css`: 触发带扩至 24px，侧栏网格和阴影动画改为 0.25s `cubic-bezier(0.4, 0, 0.2, 1)` |
| O-007 | 2026-05-21 | 侧边栏收起时，右侧工作区高度异常变大出现滚动条 | 2026-05-21 | `main.css`: `.app-shell` 固定为 `height: 100vh` 并隐藏外层溢出；`.main-panel` 补 `min-height: 0`，让工作区内容只在 `.content-panel` 内滚动 |
| O-008 | 2026-05-22 | 大图浏览模式下鼠标移动时在图片下方弹出板材关联信息（材质、规格等），鼠标停止移动后信息自动收回并隐藏，需添加平滑的显示/隐藏动画效果 | 2026-05-22 | 大图预览中鼠标移动显示板材品牌、材质、颜色、规格、尺寸信息，停止移动约 1.5 秒后淡出 |
| O-009 | 2026-05-22 | 板材展示页纹理列表列改为缩略图展示（替代当前纯文字"查看"链接）；无纹理图片的板材显示"暂无"提示文字 | 2026-05-22 | `BoardTextureCell.vue` 展示缩略图、无图占位和加载失败占位；`BoardsView.vue` 接入纹理列 |
| O-010 | 2026-05-22 | 点击纹理缩略图弹出大图查看（lightbox/模态框），仅对已有纹理图片的板材可触发 | 2026-05-22 | `BoardTextureLightbox.vue` 提供自定义大图预览；仅有可加载缩略图时触发，不再跳转新标签页 |
| O-011 | 2026-05-22 | 删除客户或板材时，已有订单关联该客户/板材，需提示并保护订单数据 | 2026-05-22 | `TCustomerServiceImpl`/`TBoardServiceImpl`: 客户被订单引用、板材被订单明细或余料引用时拒绝物理删除并返回业务提示；网页端和小程序端删除确认文案同步说明保护规则 |
| O-012 | 2026-05-22 | 大图模式支持缩放 | 2026-05-22 | `BoardTextureLightbox.vue`: 滚轮缩放（以鼠标为中心，0.5x~5x）、拖拽平移（缩放后启用）、双击重置、ESC 关闭、切换板材时状态重置 |
| O-013 | 2026-05-22 | 3D 建模区操作后摄像机频繁复位，打断用户操作节奏 | 2026-05-22 | `useThreeScene.js`: 提取 `fitCameraToBoards()`，`buildCabinet` 新增 `resetCamera` 参数；`CabinetDesignView.vue`: `buildScene` 新增 `resetCamera` 参数，板件增删改/撤销/重做/板材映射不再复位，仅柜体级操作（生成/切换/复制/删除草稿、页面加载、用户主动按 R）触发复位 |
