# 需求文档

> 只记录待处理事项；完成后移到 `需求文档-已完成.md`。
> 下一编号：Bug `B-012`，新功能 `F-013`，优化 `O-021`。

## 用户自由记录区

> 在这里随便写：测试项目时发现的任何问题、一闪而过的优化想法、未来想加的功能
> 由Agent把内容归类到下方结构化分区。

（空）

## 一、Bug

> 以下由 AI 维护，请勿手动编辑。

| 编号 | 时间 | 描述 | 状态 | 备注 |
|------|------|------|------|------|

---

## 二、新功能需求

| 编号 | 时间 | 描述 | 状态 | 备注 |
|------|------|------|------|------|
| F-004 | 2026-05-21 | 3D 柜体设计页改为左侧柜体/模板、中间 3D 预览、右侧属性面板的工作台布局 | 待验收 | 已补右侧当前柜体、选中板件、板材映射信息 |
| F-006 | 2026-05-22 | 3D 柜体设计增加更多柜型模板 | 待规划 | 在 `CabinetDesignView.vue` 的 `generateCabinetJson` 增加单门柜、双门柜、抽屉柜、吊柜等预设模板；用于展示 3D 预览能力，不要求完整自由建模和生产级设计输出 |
| F-010 | 2026-05-23 | 涉及跨账号记录的业务操作需能正确读取目标记录 | 待梳理 | 当修改数据库记录涉及不同账号下的数据时，系统不能只依赖当前登录用户的 `user_id` 租户过滤；应按业务关系显式读取目标记录，例如生产任务分配按 `assignee_id` 查看员工任务、按 `order_id` 回显订单分配信息，同时保留权限校验与业务边界，防止读不到合法关联记录或越权读取无关记录 |

---

## 三、优化想法

| 编号 | 时间 | 描述 | 状态 | 备注 |
|------|------|------|------|------|
| O-019 | 2026-05-22 | 板材下拉列表按板材类型分组展示 | 待规划 | 先用于 3D 柜体设计中的板材映射下拉，按板材类型（如”杉木芯生态板””PET饰面板”）分组，用户展开分组后选择具体板材；同样适用于系统内其他板材选择场景，例如加工数据输入页原材料选择，以避免板材数量较多时全部混在同一列表中不便查找 |
| O-020 | 2026-05-23 | 删除侧边栏收缩效果 | 待规划 | 网页端 `AppShell.vue` 移除侧边栏边缘唤出/延迟收起/防抖等收缩交互，改为固定展开或简化为常驻菜单；`main.css` 清理收缩相关触发带、过渡和布局样式 |

---

## 四、暂不处理

| 编号 | 时间 | 描述 | 原因 |
|------|------|------|------|
| F-005 | 2026-05-21 | 3D 柜体设计规划逻辑按调研资料改为"参数化模板优先，自由拼装后置" | 当前阶段改为功能展示为主，不继续扩展为完整可落地的生产设计功能；生产级自由建模能力暂不处理 |
| O-014 | 2026-05-22 | 3D 建模支持鼠标拖拽移动已有板件 | 当前阶段 3D 柜体设计以功能展示为主，不继续扩展为完整可落地的生产设计功能 |
| O-015 | 2026-05-22 | 3D 建模补充板件旋转 UI 和放置面智能投射 | 当前阶段 3D 柜体设计以功能展示为主，不继续扩展为完整可落地的生产设计功能 |
