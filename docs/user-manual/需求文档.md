# 需求文档

> 记录待处理的想法、问题和需求。已完成项归档至 `需求文档-已完成.md`。
> 编号接续已完成文档（B-006 起、F-004 起、O-004 起）。

## 用户自由记录区

> 在这里随便写——使用浏览器时发现的任何问题、一闪而过的优化想法、未来想加的功能，不用管格式。
> AI 会定期把内容归类到下方结构化分区。




---

## 一、Bug

> 以下由 AI 维护，请勿手动编辑。

| 编号 | 时间 | 描述 | 状态 | 备注 |
|------|------|------|------|------|
| B-006 | 2026-05-20 | 柜体建模中顶板/底板与左右侧板重叠，实际应按内宽嵌入 | 待验收 | 2026-05-21 已将预设生成逻辑调整为 `W - 2T`，需前端构建和浏览器确认 |
| B-007 | 2026-05-21 | 上传纹理图片失败，前端显示 upload texture failed | 待确认 | **根因分析**：最可能是 OSS 配置未填写——`application-local.yml` 无 OSS 配置，`BoardTextureStorageServiceImpl:101-105` 的 `ensureOssConfigured()` 检查四个字段为空即抛异常。、OSS 权限过期（:74 捕获 `OSSException|ClientException`）。**关键文件**：`BoardTextureStorageServiceImpl.java:101-105`、`BoardsController.java:111-124`、`BoardsView.vue:225-239`。**修复建议**：② Controller :121 IOException 日志级别提升为 error 并向前端返回具体错误信息 |

---

## 二、新功能需求

| 编号 | 时间 | 描述 | 状态 | 备注 |
|------|------|------|------|------|
| F-004 | 2026-05-21 | 3D 柜体设计页改为左侧柜体/模板、中间 3D 预览、右侧属性面板的工作台布局 | 待验收 | 已补右侧当前柜体、选中板件、板材映射信息 |
| F-005 | 2026-05-21 | 3D 柜体设计规划逻辑按调研资料改为"参数化模板优先，自由拼装后置" | 已记录 | **MVP4 实现范围分析**：规格文档定义 MVP4 为"自由拼装、吸附对齐、更多柜型、快捷键/撤销重做增强"。**已完成**：板件拖拽入库（:688-712）、三轴吸附对齐（:559-612）、复制/删除（:734-748）、撤销/重做（:763-773）、快捷键 Ctrl+Z/Y/C/Delete/R（:784-813）、视角复位（:775-777）、右侧面板属性编辑（:1269-1414）。**尚需新增**：① P0 3D 拖拽移动（当前仅数值微调，缺鼠标拖拽沿轴移动）；② P1 更多柜型（吊柜/书柜/鞋柜的 `generateCabinetJson` 分支）；③ P2 板件旋转 UI + 放置面智能投射。**关键文件**：`useThreeScene.js`（拖拽/旋转）、`CabinetDesignView.vue`（柜型生成/交互）、`cabinetDesign.js`（undo 栈）。**结论**：MVP4 增量较小，核心缺口是 3D 拖拽移动和更多柜型 |

---

## 三、优化想法

| 编号 | 时间 | 描述 | 状态 | 备注 |
|------|------|------|------|------|
| O-004 | 2026-05-21 | 3D 设计页后续补充视角复位、复制/删除快捷键、撤销/重做等高频操作 | 待规划 | 来源调研资料中酷家乐、三维家等常见交互习惯 |
| O-005 | 2026-05-21 | 右侧属性面板后续按尺寸、材质、封边/孔位等工艺分组并支持折叠 | 待规划 | 当前先展示核心只读信息，后续再扩展编辑能力 |

---

## 四、暂不处理

| 编号 | 时间 | 描述 | 原因 |
|------|------|------|------|
